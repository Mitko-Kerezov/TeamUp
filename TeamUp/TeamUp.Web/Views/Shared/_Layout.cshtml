<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - TeamUp</title>
    @Styles.Render(string.Format("~/Content/BootstrapThemes/{0}.css", ViewBag.ThemeChosen.ToString()))
    @Styles.Render("~/Content/kendo")
    @Styles.Render("~/Content/site.css")

    @RenderSection("adminscripts", required: false)
    @Scripts.Render("~/bundles/modernizr")
    @Scripts.Render("~/bundles/jquery")
</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink("TeamUp", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    @if (Request.IsAuthenticated)
                    {
                        <li>@Html.ActionLink("Projects", "Index", "Projects", new { area = "" }, new { })</li>
                        <li>@Html.ActionLink("Users", "Index", "Users", new { area = "" }, new { })</li>
                        <li>@Html.ActionLink("My messages", "Index", "Messages", new { area = "" }, new { })</li>
                        if (this.User.IsInRole("Admin"))
                        {
                            <li>@Html.ActionLink("Manage all skills", "Index", "ManageSkills", new { area = "Administration" }, new { })</li>
                            <li>@Html.ActionLink("Manage all categories", "Index", "ManageCategories", new { area = "Administration" }, new { })</li>
                        }
                    }
                </ul>
                @Html.Partial("_LoginPartial")
            </div>
        </div>
    </div>
    <div class="container body-content">
        <p class="text-success">@ViewBag.StatusMessage</p>
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - TeamUp</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/bootstrap")
    @Scripts.Render("~/bundles/kendogrid")
    @RenderSection("scripts", required: false)
</body>
</html>
