@using TeamUp.Models
@model ICollection<Project>
@if (Model.Count == 0)
{
    <p class="text-muted">None</p>
}
else
{
    foreach (var project in Model)
    {
        @Html.ActionLink(project.Name, "Details", "Projects", new { id = project.Id }, new { @class = "btn btn-primary" })
    }
}